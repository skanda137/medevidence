# Postgres
DATABASE_URL=postgres://medevidence:medevidence@localhost:5432/medevidence

# Anthropic API (for RAG generation)
ANTHROPIC_API_KEY=sk-ant-xxxx

# Embedding provider (pick one; RAG pipeline is provider-agnostic, see services/embeddings.ts)
OPENAI_API_KEY=sk-xxxx
EMBEDDING_MODEL=text-embedding-3-large

# Auth (Clerk) - wire up in Phase 0, stubbed for now
CLERK_SECRET_KEY=

PORT=4000
